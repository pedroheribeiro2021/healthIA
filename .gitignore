# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
dist/
build/
.expo/
*.tsbuildinfo

# Dados locais (nunca versionar dados de saúde)
*.db
*.db-wal
*.db-shm
*.sqlite*
.healthai/

# Ambiente / segredos
.env
.env.*
!.env.example

# SO / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Claude Code local
.claude/

# Residuo nao versionado do scaffold Vite da v1 (server Python + dashboard
# React), arquivada na branch legacy-local e removida de main (a0b7d24).
# Nao faz parte da arquitetura atual (ver CLAUDE.md) — nunca deve voltar.
/dashboard/
